# Kartawarta Feature Migration — Progress Tracker

> **Project:** Kartawarta v2.0. Sumber referensi fitur: `docs/FEATURE_REFERENCE.md` (diambil dari dokumen master JHB).
> **Cara pakai:** File ini dibaca & ditulis ulang oleh `migration-lead` agent tiap sesi.
> **Status:** `[ ]` todo · `[~]` in_progress · `[x]` done · `[!]` blocked · `[skip]` tidak berlaku
>
> Saat user bilang "lanjut", "lanjutkan", atau "kerjakan migrasi fitur":
> 1. `migration-lead` baca file ini
> 2. Pick task `[ ]` berikutnya (ikuti urutan fase — tidak loncat kecuali dependensi sudah OK)
> 3. Delegasi ke sub-agent yang tepat
> 4. Setelah sub-agent lapor selesai, orchestrator update status di sini ke `[x]`
> 5. Tambahkan bullet "Notes" kalau ada catatan (file yang dibuat, breaking change, blocker)
>
> **Dependensi antar fase:** Phase 1 (DB) harus selesai dulu sebelum Phase 2–7 bisa query data baru.
> Phase 3 (AI) harus selesai sebelum Phase 4 (Social) & Phase 6 (SEO auto-gen Sorotan/FAQ).

---

## Phase 0 · Foundation
- [x] Save `docs/FEATURE_REFERENCE.md`
- [x] Set up `docs/MIGRATION_PROGRESS.md`
- [x] Create agent roster (`migration-lead` + 9 sub-agent: ai-client-builder, seo-distributor, social-publisher, social-template-renderer, analytics-connector, cloudflare-ops, cron-engineer, integration-secrets-ui, doc-panel-builder)
- [x] Update `.claude/agents/README.md`
- [x] Update `CLAUDE.md` (termasuk fix palet outdated: goto green → navy)
- [x] Save project memory entry (`feature_migration_project.md`)

## Phase 1 · Database Schema Expansion
*Agent: `database-architect` (via `migration-lead`)*

- [x] Tambah model `Sorotan` (id, slug, articleId, angle: KRONOLOGI|ANALISIS|DAMPAK, title, content, indexStatus, lastIndexedAt)
- [x] Tambah model `SocialPost` (id, articleId, platform, status, externalId, imageUrl, caption, publishedAt, errorMessage, deletedAt)
- [x] Tambah model `SocialTemplate` (id, name, platform, categoryId?, backgroundUrl, textLayers Json, isActive)
- [x] Tambah model `SocialMediaSettings` (id, draftMode, autoPublishIG, autoPublishFB, autoPublishTwitter, defaultHashtags, defaultCTA)
- [x] Tambah model `InstagramSettings` (accessToken, igUserId, templateDefaultId, enabled, captionMaxLen, hashtagCount)
- [x] Tambah model `FacebookSettings` (pageId, accessToken, postMode, templateDefaultId, enabled)
- [x] Tambah model `CourtSchedule` (id, caseName, caseNumber, courtName, scheduledAt, status, notes)
- [x] Tambah model `TargetKeyword` (id, keyword, categoryId?, priority, isActive, lastGeneratedAt)
- [x] Tambah model `CtaTemplate` (id, name, html, position, isActive)
- [x] Expand `Article` dengan: `coAuthors`, `isAutoGenerated`, `sourceArticleId`, `publishToInstagram`, `publishToFacebook`, `publishToTwitter`, `socialCaptions` Json, `faqData`, `indexStatus`, `lastIndexedAt`
- [x] Add relation `Article.sorotan Sorotan[]`, `Article.socialPosts SocialPost[]`
- [x] `npx prisma format && npx prisma generate` — selesai. `npx prisma db push` ditinggal ke user (push ke production VPS)
- [x] Deploy ke VPS: `git pull && prisma generate && prisma db push && npm run build && pm2 restart kartawarta` — sukses 2026-04-24, PM2 `kartawarta` online
- [skip] Smoke test manual (buat 1 Sorotan/SocialPost di prisma studio) — skip; akan tervalidasi otomatis saat Phase 3/4 bikin record beneran

## Phase 2 · AI Shared Client
*Agent: `ai-client-builder`*

- [x] Buat `src/lib/ai-client.ts` — `callAI()` dengan Anthropic (Claude Haiku 4.5) primary + DeepSeek fallback (~270 LoC)
- [x] Baca API key dari `SystemSetting` + env fallback (`ANTHROPIC_API_KEY`, `DEEPSEEK_API_KEY`)
- [x] Fire-and-forget log ke `AIUsageLog` (inputTokens, outputTokens, feature, userId, articleTitle)
- [x] Install `@anthropic-ai/sdk@^0.90.0` (3 packages)
- [x] Refactor `src/app/api/ai/generate/route.ts` pakai `callAI()` — response schema tambah field `provider` (additive)
- [x] Unit test `src/lib/__tests__/ai-client.test.ts` — 5/5 pass (fallback + forced provider + both-fail cases)

## Phase 3 · SEO Distribution (Google Indexing + IndexNow + Sorotan + JSON-LD)
*Agent: `seo-distributor`*

- [x] Install `googleapis` (25 packages)
- [x] `src/lib/seo/google-indexing.ts` — `submitUrlToGoogle()` + `testGoogleCredentials()` via service account JWT
- [x] `src/lib/seo/indexnow.ts` — `pingIndexNow()` + `getIndexNowKey()` dengan fallback file→SystemSetting→env
- [x] IndexNow key generated + `public/indexnow-key.txt` + `public/{key}.txt` (spec-compliant)
- [x] `src/lib/seo/sorotan-generator.ts` — 3 angle (KRONOLOGI/ANALISIS/DAMPAK), prompt Indonesia, consume `callAI`
- [x] `src/lib/seo/json-ld.ts` — 8 builder: newsArticle, article, breadcrumb, faq, howTo, qa, organization, website
- [x] Expand `seo-auto.ts` `onArticlePublished(slug, articleId?)` — Promise.allSettled: Indexing + IndexNow + Sorotan; Cloudflare purge placeholder `TODO(phase-6)`; writes AuditLog `ARTICLE_PUBLISHED_SEO_CHAIN`
- [x] `/sitemap-news.xml` — Google News format, 48 jam, max 1000 artikel, dengan `<news:keywords>` dari tags
- [x] `/sorotan/[slug]` — detail + Article/Breadcrumb/Organization JSON-LD + sidebar "other angles"
- [x] `/sorotan` — list dengan angle filter + pagination
- [x] Inject JSON-LD di `/berita/[slug]`: NewsArticle + BreadcrumbList + FAQPage (kalau `faqData` ada)
- [x] 10 API endpoints di `/api/seo/*`: submit, status, generate-sorotan, generate-sorotan-single, batch-index, bulk-reindex, test-credentials, sorotan-status (GET+POST), ping (cron CRON_SECRET)

## Phase 4 · Social Media Automation
*Agents: `social-publisher` + `social-template-renderer`*

- [x] Install `sharp@^0.34.0` (3 packages)
- [x] `src/lib/social/types.ts` — Platform, PublishStatus, PublishResult, PreparedPost, ArticleForPublish, TextLayer, PLATFORM_DIMENSIONS, CAPTION_MAX_LENGTH
- [x] `src/lib/social/template-renderer.ts` — Sharp composite + SVG text overlay + placeholder {title}/{summary}/{category}/{date}/{author} + JPEG q85
- [x] `src/lib/social/template-helper.ts` — findTemplateForPlatform, renderAndStoreTemplate (simpan ke `public/uploads/social/`), enrichArticleForTemplate
- [x] `src/lib/social/ai-caption.ts` — generateCaptionForTemplate via callAI feature "social_caption"
- [x] `src/lib/social/caption-generator.ts` — generateSocialCaption dengan hashtag + CTA + enforced max length per platform
- [x] `src/lib/social/instagram.ts` — InstagramPublisher 2-step Meta Graph v21 (/media → /media_publish), error mapping 190/100/368
- [x] `src/lib/social/facebook.ts` — FacebookPublisher link_share + photo + deletePost untuk takedown
- [x] `src/lib/social/orchestrator.ts` — publishArticleToSocial, approveDraft, rejectDraft (hapus file), takedownPost; auto-bootstrap singleton settings row id="global"
- [x] 15 API endpoints di `/api/social/*` (posts + approve/reject/mark-deleted/takedown, settings GET/PUT, templates CRUD + preview, preview, test-publish SUPER_ADMIN)
- [skip] Panel `/panel/social` — DIPINDAH ke Phase 8 (bundling semua panel UI)
- [x] Hook `publishArticleToSocial` dari `onArticlePublished` (non-blocking, respect draftMode)

## Phase 5 · External Analytics (GA4 + GSC + Cloudflare)
*Agent: `analytics-connector`*

- [x] `src/lib/stats/google-analytics.ts` — GA4 Data API v1beta, 3 parallel runReport (totals + topPages + dailyTrend)
- [x] `src/lib/stats/google-search.ts` — GSC API v1, 4 parallel searchanalytics.query (totals + queries + pages + daily)
- [x] `src/lib/stats/cloudflare.ts` — CF GraphQL httpRequests1dGroups, cacheHitRate = cachedRequests/requests
- [x] `src/lib/stats/internal.ts` — Prisma queries: articles/users/views/comments/polls/AI/sorotan/social + 7-day trend bucket
- [x] 4 API endpoints di `/api/stats/*` — EDITOR+ auth, `?from=&to=`, default 30 hari, in-memory cache TTL 5 min
- [skip] Panel `/panel/statistik` + `/panel/statistik-editor` — DIPINDAH ke Phase 8 (bundling UI)
- [skip] Install `recharts` — akan di-install saat Phase 8 (UI consumer)

## Phase 6 · Cloudflare Ops (cache purge otomatis)
*Agent: `cloudflare-ops`*

- [x] `src/lib/cloudflare/purge.ts` — `purgeCache(urls)`, `purgeEverything()`, `purgeArticleCache(slug, categorySlug?)` via CF API v4 dengan token dari SystemSetting + env fallback, 15s timeout, validate absolute URLs
- [x] Hook `onArticlePublished` di `src/lib/seo-auto.ts` — purge 5-6 URL (article, home, /berita, sitemap.xml, sitemap-news.xml, category) paralel dengan Indexing + IndexNow + Sorotan + Social
- [x] `POST /api/cloudflare/purge` (SUPER_ADMIN) — manual purge, body `{urls:[]}` atau `{everything:true}`, logAudit `CACHE_PURGE`
- [x] AuditLog entry `ARTICLE_PUBLISHED_SEO_CHAIN` di `onArticlePublished` termasuk `cloudflare: {ok, purgedCount, error}`

## Phase 7 · Cron Jobs Orchestration
*Agent: `cron-engineer`*

- [x] Audit + enhance `/api/cron/publish` — `onArticlePublished` sekarang **AWAITED**, added POST method, per-article try/catch, maxDuration 300s
- [x] `/api/cron/auto-article` NEW — respect `auto_article_enabled` toggle, random TargetKeyword dari top 10 priority, callAI feature "article_draft" + fence-tolerant JSON parse + text fallback, auto-resolve admin author + category
- [x] `/api/cron/sorotan` NEW — top 5 PUBLISHED tanpa Sorotan, consume generateSorotan()
- [x] `/api/cron/seo-submit` NEW — thin alias re-export GET dari `/api/seo/ping`
- [x] `/api/cron/backup` NEW — HTTP no-op trigger + `scripts/backup-db.sh` shell script (pg_dump gzip, retensi 7 hari)
- [x] Semua cron verify `Authorization: Bearer ${CRON_SECRET}` — graceful 401 kalau invalid
- [x] `docs/DEPLOY_VPS.md` — lengkap dengan crontab template, CRON_SECRET setup, log file, backup dir, restore commands

## Phase 8 · Panel Pages (UI baru)
*Agent: `frontend-dev` (via `migration-lead`)*

- [x] `/panel/auto-artikel` — toggle `auto_article_enabled`, list artikel `isAutoGenerated=true`, placeholder TargetKeyword CRUD (butuh `/api/target-keywords`)
- [x] `/panel/social` — 3 tab (Posts/Templates/Settings), CRUD template + preview modal, per-scope settings PUT
- [x] `/panel/sorotan` — status cards, batch generate, per-article generate-single
- [x] `/panel/tags` — CRUD + sort; AI riset keyword placeholder
- [x] `/panel/statistik` — 4 tab Recharts (Internal/GA4/GSC/Cloudflare) + date range picker + graceful empty states
- [skip] `/panel/statistik-editor` — sudah existing, tidak diubah (endpoint dedicated belum dibuat)
- [x] `/panel/seo` — section "Monitor Index" di halaman existing: cards aggregate + Re-index + Test Credentials
- [x] `/panel/jadwal-sidang` — CRUD CourtSchedule + filter status + `/api/court-schedule` endpoint baru (GET public, POST/PUT JOURNALIST+, DELETE EDITOR+)
- [x] `/panel/dokumentasi` — render `docs/FEATURE_REFERENCE.md` via `marked`+`isomorphic-dompurify`, sidebar ToC + scroll spy + Print button, SUPER_ADMIN only
- [x] Install: `recharts`, `marked`, `isomorphic-dompurify`
- [x] `src/app/panel/layout.tsx` — menu items update, role gating per halaman baru

## Phase 9 · Public Pages (baru)
*Agent: `frontend-dev`*

- [x] `/sorotan` + `/sorotan/[slug]` — DONE di Phase 3
- [x] `/rangkuman` — list smart cards (pekan-ini, bulan-ini, per category)
- [x] `/rangkuman/[slug]` — slug router (pekan-ini/bulan-ini/category) + CollectionPage JSON-LD + 404 graceful
- [x] `/rangkuman/harian` — daily archive grouped 30 hari terakhir
- [x] `/rangkuman/harian/[slug]` — per tanggal YYYY-MM-DD + 404 invalid/future/empty
- [x] `/topik` — daftar Category dengan icon + count
- [x] `/topik/[slug]` — 308 redirect ke `/kategori/[slug]`
- [x] `/jadwal-sidang` — group Live/Today/Week/Later + empty state graceful
- [x] `/lokasi` — direktori 8 pengadilan Bandung dari `src/data/court-locations.ts` (force-static)
- [x] `/lokasi/[slug]` — detail + GovernmentOffice JSON-LD + upcoming schedules + related articles
- [x] `/iklan` — rate card 4 paket + specs + CTA WhatsApp/email (placeholder konten editable)
- [x] `/feed.xml` — RSS 2.0 dengan 30 items, content:encoded, atom:link self
- [x] `/api/og?slug=...` — Sharp-based OG image 1200x630 dengan Cache-Control immutable
- [x] `src/app/berita/[slug]` metadata — openGraph.images + twitter.images point ke `/api/og?slug=`

## Phase 10 · Editor Enhancements
*Agent: `frontend-dev`*

- [x] TipTap Table — Table/TableRow/TableCell/TableHeader extensions + dropdown toolbar (insert 3x3, add/del row/col, toggle header)
- [x] `ImageCropModal.tsx` — pure HTML5 canvas, drag move + 4 corner handles, 5 aspect ratio presets (16:9, 1:1, 4:5, 1.91:1, bebas), output WebP max 1600px q=0.88
- [x] Autosave 15s + beforeunload — silent server-backed save, status badge cycling (Tersimpan/Belum tersimpan/Menyimpan), owner-only DRAFT/REJECTED
- [x] Export PDF + Export TXT — sudah existing di `src/lib/export-utils.ts`, button sudah wired di edit page
- [x] AI Tools toolbar — Generate Judul/Meta/Caption Sosmed dropdown, callbacks setTitle/setSeoDescription/clipboard

## Phase 11 · Integrasi Keys & Settings UI
*Agent: `integration-secrets-ui`*

- [ ] Refactor `/panel/pengaturan` — grupkan keys per integrasi (AI, Google, Meta, Twitter, Cloudflare, Resend)
- [ ] Toggle show/hide untuk field sensitif (password-type + eye icon)
- [ ] Test button per integrasi: "Test Connection" → endpoint validasi
- [ ] API `/api/seo/test-credentials` (existing target), `/api/social/test-publish` (existing target), tambah `/api/ai/test` + `/api/email/test` kalau perlu
- [ ] Validasi input format (JSON parse untuk `google_credentials_json`)

## Phase 12 · Security Hardening
*Agent: `security-auditor` + `auth-guardian`*

- [ ] Audit semua `/api/**` — pastikan `requireAuth()`/`requireRole()` tepat
- [ ] CSP: review `'unsafe-inline'` + `'unsafe-eval'` perlunya; tambah hash/nonce kalau memungkinkan
- [ ] Rate-limit di endpoint public baru (comment, poll, report, contact, search, trending)
- [ ] Invalidate session on password change (handler di `/api/users/me` PUT)
- [ ] Check semua sanitization di input HTML (TipTap output pas POST/PUT artikel)
- [ ] Secret scan: pastikan tidak ada API key di kode
- [ ] Review CRON_SECRET usage — apakah semua cron endpoint verify

## Phase 13 · Build, Test, Commit, Deploy
*Agents: `build-test-validator` → `git-release-specialist`*

- [ ] `npm run build` pass
- [ ] `npm run lint` pass
- [ ] `npm test` (vitest) pass
- [ ] Commit dengan pesan deskriptif per phase
- [ ] Push ke `origin master`
- [ ] SSH VPS deploy
- [ ] `curl` production URL — 200 OK
- [ ] Smoke test via browser

---

## Log Sesi

> Orchestrator append ke bawah setiap kali selesai fase atau blocker.

### 2026-04-24 — Phase 0 Foundation
- Spec + progress tracker + agent roster dibuat.
- Next: Phase 1 — schema expansion via `database-architect`.

### 2026-04-24 — Phase 1 Database Schema Expansion ✅
- Delegasi ke `database-architect`. Schema expanded dengan 9 model baru, 4 enum baru, Article +10 field. `prisma validate/format/generate` sukses locally.
- **Deploy ke VPS**: user jalankan `git pull && prisma generate && prisma db push && npm run build && pm2 restart kartawarta` — sukses. PM2 `kartawarta` online.

### 2026-04-24 — Phase 10 Editor Enhancements ✅
- TipTap Table 4 extension installed + toolbar dropdown
- ImageCropModal pure HTML5 canvas (no extra dep), 5 aspect ratio presets, integrated ke ImageUploader
- Autosave 15s server-backed silent + beforeunload warning + status badge dirty/clean/saving (owner-only DRAFT/REJECTED)
- Export PDF + TXT existing already wired
- AI Tools toolbar di RichTextEditor: Generate Judul/Meta/Caption — callbacks setTitle/setSeoDescription/clipboard
- Build: tsc clean, next build pass
- Caveat: multi-tab autosave = last-write-wins (revisions still tracked)

### 2026-04-24 — Phase 9 Public Pages ✅
- 11 page baru + 1 RSS route + 1 OG image API + 1 data file
- `/rangkuman/*` smart grouping dari Article data, slug router untuk pekan-ini/bulan-ini/category
- `/topik/[slug]` redirect 308 ke `/kategori/[slug]` (avoid duplicate)
- `/jadwal-sidang` group by waktu (Live/Today/Week/Later)
- `/lokasi/*` 8 pengadilan Bandung di static data file (editable tanpa code change major)
- `/iklan` placeholder rate card (user edit string nanti)
- `/feed.xml` RSS 2.0 valid dengan content:encoded
- `/api/og?slug=...` dynamic OG image generator pakai Sharp, immutable cache 1 tahun
- JSON-LD: CollectionPage di rangkuman detail, GovernmentOffice di lokasi detail
- Build: tsc clean, next build pass

### 2026-04-24 — Phase 8 Panel UI ✅
- 9 panel page di `src/app/panel/*` (auto-artikel, social, sorotan, tags, statistik, seo-extended, jadwal-sidang, dokumentasi + menu layout update)
- 1 endpoint baru: `/api/court-schedule` (GET public, POST/PUT JOURNALIST+, DELETE EDITOR+)
- 3 deps: recharts, marked, isomorphic-dompurify
- `/panel/statistik` tab GA4/GSC/Cloudflare graceful empty state saat credentials missing
- `/panel/dokumentasi` render `FEATURE_REFERENCE.md` sebagai halaman panel dengan ToC + Print
- Build: tsc clean, next build pass
- Placeholders: `/panel/tags` AI riset keyword + `/panel/auto-artikel` TargetKeyword CRUD (butuh endpoint baru di fase lanjutan)
- `/panel/statistik-editor` existing tidak diubah

### 2026-04-24 — Phase 7 Cron Jobs Orchestration ✅
- 4 file cron endpoint NEW: auto-article, sorotan, seo-submit (alias), backup
- `/api/cron/publish` di-enhance: `onArticlePublished` AWAITED, POST added, per-article try/catch
- `scripts/backup-db.sh` shell script untuk pg_dump + retention 7 hari
- `docs/DEPLOY_VPS.md` lengkap dengan crontab template + setup instructions
- Auto-article: graceful fallback kalau AI fail (200 + error field, no crontab retry-spam)
- Sorotan: limit 5 artikel per run
- SEO submit: thin alias, reuse Phase 3 `/api/seo/ping` code (no duplication)
- Backup endpoint: HTTP no-op trigger; actual pg_dump via shell script
- Build: tsc clean, next build pass
- Prerequisite VPS: set `CRON_SECRET` di `.env` (generate dengan `openssl rand -hex 32`) + pasang crontab entries
- Next: Phase 8 — Panel UI bundling (auto-artikel, social, sorotan, statistik, statistik-editor, seo, jadwal-sidang, tags, dokumentasi)

### 2026-04-24 — Phase 6 Cloudflare Ops ✅
- `src/lib/cloudflare/purge.ts` — 3 fungsi: purgeCache (specific URLs), purgeEverything (danger), purgeArticleCache (convenience)
- Hook di `onArticlePublished`: purge 5-6 URL per publish, masuk ke Promise.allSettled fan-out
- `POST /api/cloudflare/purge` (SUPER_ADMIN) untuk manual purge
- `PublishChainSummary` ditambah field `cloudflare: {ok, purgedCount, error}`
- Build: tsc clean
- SystemSetting keys (sudah shared Phase 5): `cloudflare_api_token`, `cloudflare_zone_id`. Env fallback: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ZONE_ID

### 2026-04-24 — Phase 5 Analytics Connector ✅
- 4 library file di `src/lib/stats/` (internal, google-analytics, google-search, cloudflare)
- 4 API endpoint di `/api/stats/*` dengan EDITOR+ auth + date range + cache TTL 5 min
- Graceful fallback: credentials missing → HTTP 200 `{success:false, error, data:empty}` — UI tidak crash
- Build: tsc clean, next build pass
- SystemSetting keys: `ga4_property_id` (NEW), `gsc_site_url` (optional), `google_credentials_json` (shared Phase 3), `cloudflare_api_token` + `cloudflare_zone_id` (shared Phase 6)
- Setup prerequisite: user harus tambah service account sebagai Viewer di GA4 property + Owner di Search Console property
- Panel UI + `recharts` install di-defer ke Phase 8

### 2026-04-24 — Phase 4 Social Media Automation ✅
- 8 library file di `src/lib/social/` (types, template-renderer, template-helper, ai-caption, caption-generator, instagram, facebook, orchestrator)
- 15 API endpoint di `/api/social/*` (11 file route, beberapa handle GET+POST)
- `onArticlePublished` di-hook dengan `publishArticleToSocial` (non-blocking, default draftMode=true)
- Dependencies: `sharp@^0.34.0`
- Build: tsc clean, next build pass
- **Default draftMode=true** — artikel baru tidak auto-post sampai admin approve, safe first-run
- SystemSetting / DB rows untuk aktivasi:
  - `InstagramSettings` id="global": accessToken, igUserId, enabled=true
  - `FacebookSettings` id="global": pageId, accessToken, postMode, enabled=true
  - `SocialMediaSettings` id="global": draftMode, autoPublishIG/FB, defaultHashtags, defaultCTA
  - Minimal 1 `SocialTemplate` aktif per platform
  - Singleton rows auto-bootstrap default kalau belum ada
- Limitations: IG takedown flag-only (Graph API tidak support delete), Meta token expire ~60 hari (refresh manual atau wait Phase 11 settings UI), font custom belum embedded di SVG
- Twitter publisher out of scope Phase 4 (enum tersedia, publisher return not-implemented kalau di-call)
- Panel UI `/panel/social` di-defer ke Phase 8

### 2026-04-24 — Phase 3 SEO Distribution ✅
- 4 library file baru di `src/lib/seo/` + IndexNow key di `public/`
- 2 halaman publik `/sorotan` + `/sorotan/[slug]`
- 1 route `/sitemap-news.xml`
- 10 API endpoint di `/api/seo/*` (9 path, sorotan-status GET+POST)
- `seo-auto.ts` `onArticlePublished` di-rewrite dengan fan-out Indexing + IndexNow + Sorotan generator
- JSON-LD NewsArticle + BreadcrumbList + FAQPage di `/berita/[slug]`
- Dependencies: `googleapis`
- Build: tsc clean, next build pass
- SystemSetting keys untuk aktivasi: `google_credentials_json`, `google_indexing_enabled`
- Cloudflare purge hook sudah disiapkan dengan `TODO(phase-6)` comment
- `/api/seo/ping` siap di-wire ke crontab oleh Phase 7

### 2026-04-24 — Phase 2 AI Shared Client ✅
- Delegasi ke agent (spec: `.claude/agents/ai-client-builder.md`). Hasil:
  - `src/lib/ai-client.ts` — exports `callAI()` dengan Anthropic primary + DeepSeek fallback + fire-and-forget AIUsageLog
  - Type ekspor: `AIFeature`, `CallAIOptions`, `CallAIResult`
  - API key resolution: `SystemSetting` → env fallback. Model override via env (`ANTHROPIC_MODEL`, `DEEPSEEK_MODEL`)
  - Timeout 60s via AbortController per provider
  - `src/app/api/ai/generate/route.ts` di-refactor pakai `callAI()`, tambah field `provider` di response (additive)
  - Test: 5/5 pass + full suite 40/40 pass
  - Build: `tsc --noEmit` clean, Next.js compile + lint pass (prerender error di local dev cuma karena .env user stale — tidak affect VPS build)
- SystemSetting keys yang user perlu set nanti: `anthropic_api_key`, `deepseek_api_key`
- Next: Phase 3 — SEO Distribution (Google Indexing + IndexNow + Sorotan generator + JSON-LD + news sitemap). Perlu `googleapis` dep dan service account JSON dari user.

### 2026-04-24 — Phase 1 Database Schema Expansion
- Semua 9 model baru ditambahkan ke `prisma/schema.prisma` (additive, non-breaking).
- Article expanded dengan 10 field baru + 2 relasi balik.
- 4 enum baru: `SorotanAngle`, `SocialPlatform`, `SocialPostStatus`, `CourtScheduleStatus`.
- `prisma validate` — OK. `prisma format` — OK. `prisma generate` — client v5.22.0 regenerated.
- `prisma db push` BELUM dijalankan — user harus push manual ke VPS production.
- Next: Phase 2 — AI Shared Client (`ai-client-builder`).

---

## Catatan Teknis & Decision Log

- **Brand:** Tetap Kartawarta (kartawarta.com). Dokumentasi menyebut JHB karena sumbernya dari jurnalishukumbandung.com. Dalam kode, pakai constant `src/lib/constants.ts` untuk domain/brand.
- **Role:** Kartawarta pakai 6 role (bukan 4 seperti dokumentasi). Editor group = `{CHIEF_EDITOR, EDITOR}`. Writer group = `{SENIOR_JOURNALIST, JOURNALIST, CONTRIBUTOR}`. Sudah ada helper `canPublishDirectly`, `canApproveArticles`, `canWriteArticles`, `canManageUsers`, `canManageAds` di [src/lib/auth.ts](../src/lib/auth.ts).
- **Design:** Tetap palet navy `#002045` (Editorial Authority) — JANGAN ikuti dokumentasi yang masih bilang green. Alias `goto.green` → `#002045` dipertahankan untuk migrasi bertahap.
- **AI primary:** Claude Haiku 4.5 (model id: `claude-haiku-4-5`). Fallback: DeepSeek (`deepseek-chat`). Keduanya readable dari `SystemSetting` dengan env fallback.
- **Meta Graph API version:** v21.
- **PM2 process name:** `kartawarta` (bukan `jhb`).
- **App dir VPS:** `/var/www/kartawarta`.
